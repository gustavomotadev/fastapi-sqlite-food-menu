<div class="container mt-3">
    <div class="row">
        {% for item in produtos %}
            {% set produto = item %}
            <div class="mt-3 mb-3 col-md-4">
                {% include "card_produto.jinja" %}
            </div>
        {% endfor %}
    </div>
</div>